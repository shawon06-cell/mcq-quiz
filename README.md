# MCQ Quiz
Flutter দিয়ে তৈরি বাংলা MCQ Quiz App।

## APK বানাতে
1. Flutter SDK ও Android Studio ইনস্টল করুন।
2. এই project folder Android Studio/VS Code-এ খুলুন।
3. `flutter pub get` চালান।
4. `flutter build apk --release` চালান।
5. APK পাবেন `build/app/outputs/flutter-apk/app-release.apk` এ।
